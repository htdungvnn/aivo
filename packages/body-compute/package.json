{
  "name": "@aivo/body-compute",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic body metrics calculations and transformations",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "type-check": "pnpm --filter @aivo/shared-types run build && tsc --noEmit",
    "test": "pnpm --filter @aivo/shared-types run build && tsc --noEmit && echo 'Type check passed - body-compute is valid'"
  },
  "dependencies": {
    "@aivo/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@aivo/jest-config": "workspace:*",
    "typescript": "^6.0.3"
  }
}
