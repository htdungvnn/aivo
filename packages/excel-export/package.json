{
  "name": "@aivo/excel-export",
  "version": "0.1.0",
  "description": "Excel, CSV, JSON export utilities for AIVO platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @aivo/shared-types run build && tsc",
    "type-check": "pnpm --filter @aivo/shared-types run build && tsc --noEmit",
    "test": "vitest --passWithNoTests"
  },
  "dependencies": {
    "@aivo/shared-types": "workspace:*",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/xlsx": "^0.0.36",
    "typescript": "^6.0.3",
    "vitest": "^1.0.0"
  }
}
