{
  "name": "@aivo/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic API client for AIVO",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "type-check": "pnpm --filter @aivo/shared-types run build && tsc --noEmit",
    "test": "pnpm --filter @aivo/shared-types run build && tsc --noEmit && echo 'Type check passed - api-client is valid'"
  },
  "dependencies": {
    "@aivo/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^6.0.3"
  }
}
