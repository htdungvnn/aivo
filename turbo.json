{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env*.local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", ".expo/**"]
    },
    "build:wasm": {
      "dependsOn": ["^build"],
      "outputs": ["pkg/**", "*.wasm"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "type-check": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "clean": {
      "cache": false
    }
  }
}
